/// 앱 전체에서 사용되는 크기 및 여백 상수 정의
class AppDimensions {
  AppDimensions._();

  // 페이지 여백
  static const double pageHorizontal = 16.0;
  static const double pageVertical = 16.0;

  // 카드 및 컨테이너 여백
  static const double cardPadding = 16.0;
  static const double cardMargin = 8.0;

  // 리스트 아이템 여백
  static const double listItemPadding = 16.0;
  static const double listItemSpacing = 8.0;

  // 버튼 여백
  static const double buttonPaddingHorizontal = 24.0;
  static const double buttonPaddingVertical = 12.0;
  static const double buttonHeight = 48.0;

  // 입력 필드 여백
  static const double inputPaddingHorizontal = 16.0;
  static const double inputPaddingVertical = 12.0;
  static const double inputHeight = 48.0;

  // 일반적인 여백 크기
  static const double spacingXs = 4.0;   // 매우 작은 여백
  static const double spacingSm = 8.0;   // 작은 여백
  static const double spacingMd = 16.0;  // 중간 여백
  static const double spacingLg = 24.0;  // 큰 여백
  static const double spacingXl = 32.0;  // 매우 큰 여백

  // Border Radius (모서리 반경)
  static const double radiusXs = 4.0;    // 매우 작은 반경
  static const double radiusSm = 8.0;    // 작은 반경
  static const double radiusMd = 12.0;   // 중간 반경
  static const double radiusLg = 16.0;   // 큰 반경
  static const double radiusXl = 24.0;   // 매우 큰 반경
  static const double radiusCircular = 50.0; // 원형

  // 아이콘 크기
  static const double iconXs = 16.0;     // 매우 작은 아이콘
  static const double iconSm = 20.0;     // 작은 아이콘
  static const double iconMd = 24.0;     // 중간 아이콘
  static const double iconLg = 32.0;     // 큰 아이콘
  static const double iconXl = 48.0;     // 매우 큰 아이콘

  // Elevation (그림자 높이)
  static const double elevationNone = 0.0;
  static const double elevationSm = 2.0;
  static const double elevationMd = 4.0;
  static const double elevationLg = 8.0;
  static const double elevationXl = 16.0;

  // Border Width (테두리 두께)
  static const double borderWidthThin = 1.0;
  static const double borderWidthThick = 2.0;

  // 앱바 높이
  static const double appBarHeight = 56.0;
  
  // 바텀 네비게이션 높이
  static const double bottomNavHeight = 60.0;

  // 최소 터치 타겟 크기 (접근성)
  static const double minTouchTarget = 44.0;
}